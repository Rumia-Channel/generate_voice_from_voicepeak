[package]
name = "generate_voice_from_voicepeak"
version = "0.1.0"
edition = "2024"

[dependencies]
vpsdk = { git = "https://gitlab.com/voicepeak_mods/VOICEPEAK_SDK_RUST.git"}

serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"